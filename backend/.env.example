# Environment Configuration
NODE_ENV=development
PORT=3001

# Database
DATABASE_URL="postgresql://inomad:inomad_password@localhost:5432/inomad_khural?schema=public"

# Government Services Databases (separate for data isolation)
MIGRATION_SERVICE_DATABASE_URL="postgresql://inomad:inomad_password@localhost:5432/inomad_migration?schema=public"
ZAGS_SERVICE_DATABASE_URL="postgresql://inomad:inomad_password@localhost:5432/inomad_zags?schema=public"
LAND_REGISTRY_DATABASE_URL="postgresql://inomad:inomad_password@localhost:5432/inomad_land_registry?schema=public"

# Blockchain Configuration
BLOCKCHAIN_ENABLED=true
ALTAN_RPC_URL="http://localhost:8545"
ALTAN_CHAIN_ID=31337

# Contract Addresses Path (relative to backend/)
CONTRACTS_BASE_PATH="../chain"

# Authentication
# Generate with: node -e "console.log(require('crypto').randomBytes(48).toString('base64'))"
AUTH_JWT_SECRET=
JWT_SECRET=
AUTH_JWT_EXPIRY=1h

# MPC Wallet Server Share Encryption Key
# Generate with: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
SERVER_SHARE_KEY=

# CORS
CORS_ORIGIN="http://localhost:3000"

# Migration Service - Document Encryption
# Generate with: node -e "console.log(require('crypto').randomBytes(32).toString('base64'))"
MIGRATION_MASTER_KEY=
DOCUMENT_STORAGE_PATH="./storage/encrypted_documents"
