import PlaceholderPage from "../_components/placeholder-page";

export default function RegistriesPage() {
  return <PlaceholderPage title="Registries" description="Identity registry, elections, property records, and contract management." />;
}
